apiVersion: v1
kind: Service
metadata:
  name: nginx2-service
spec:
  selector:
    app: second-nginx
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
#    nodePort: 30023
  type: ClusterIP

